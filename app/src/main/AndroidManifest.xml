<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.olutapp_v2">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application android:theme="@style/AppTheme">
        <activity android:name=".BeerClickedOluetActivity"></activity>
        <activity
            android:name=".BeerClickedActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RestaurantClickedActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RegisterActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ChatActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SuosikitActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SuositutActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RavintolatActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".OluetActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.login.MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>